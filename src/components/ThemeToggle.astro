<input type="checkbox" id="theme__toggle" /><label for="theme__toggle"></label>
<style is:inline>
	input[type="checkbox"] {
		height: 0;
		width: 0;
		visibility: hidden;
	}

	label {
		cursor: pointer;
		width: 28px;
		height: 22px;
		background: var(--clr-bg);
		display: inline-block;
		position: relative;
		outline: 1px solid;
	}

	label:after {
		content: "";
		position: absolute;
		top: 2px;
		left: 2px;
		width: 12px;
		height: 18px;
		background: var(--clr-text);
		transition: 0.3s;
	}

	input:checked + label {
		background: var(--clr-bg);
	}

	input:checked + label:after {
		left: calc(100% - 2px);
		transform: translateX(-100%);
	}
</style>

<style is:global>
	body.darkmode {
		--clr-bg: #121212;
		--clr-text: white;
	}
</style>
